name: Goal 03 Legacy Browser Gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Goal 03 v1.2 browser gate is superseded by:'
          echo '  .github/workflows/goal3-v13-signoff.yml'
          echo '  .github/workflows/goal3-v13-public.yml'
          echo 'Use those gates for Goal 03 v1.3 and later.'
